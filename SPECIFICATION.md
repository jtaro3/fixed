# CORE DEFENSE 仕様書

| 項目 | 内容 |
| --- | --- |
| プロダクト名 | CORE DEFENSE |
| 現行バージョン | v1.2.4 |
| 種別 | スマホ・PC対応のブラウザ防衛ゲーム |
| 公開先 | GitHub Pages |
| 基準実装 | `index.html` / `styles.css` / `game.js` |

## 1. コンセプト

画面中央で固定されたコア（○）を、迫ってくる敵（×）から守るタップゲーム。

プレイヤーは敵を素早くタップして耐久を減らし、コアに触れる前に破壊する。画面上の情報量は少なく保ち、敵・コア・操作状態を一目で判断できることを重視する。

## 2. ゲーム仕様

### 2.1 基本ルール

- コア（○）はゲームエリアの中央に固定され、移動しない。
- ゲーム開始直後に敵を1体出現させる。
- その後は2秒ごとに敵の出現を試みる。
- 敵は出現後、操作なしでコアへ向かって移動する。
- 敵をタップすると耐久が1減る。耐久が0になった敵は破壊され、`DESTROYED` が1増える。
- 敵がコアに接触した時点でゲーム終了。
- 画面上の敵が難易度別の上限に達している間は、新たな敵を出現させない。

### 2.2 出現位置

- 敵はコアから十分に離れた位置に出現する。
- コア・敵の半径に加え、最低78px相当の余白を設ける。
- 画面端から28px以上内側の位置に限定する。
- 他の敵と近接しすぎる候補は除外する。

この制約により、出現した瞬間にコアと接触してゲーム終了することを防ぐ。

### 2.3 敵の耐久と色

敵は出現時に、耐久1〜5をランダムに持つ。タップごとに耐久が1減り、現在の耐久に応じて敵の色も変化する。耐久の数字はゲーム画面には表示しない。

| 現在耐久 | 色 | カラーコード |
| --- | --- | --- |
| 1 | 青 | `#54A6FF` |
| 2 | 緑 | `#62D98B` |
| 3 | 黄 | `#FFD85B` |
| 4 | 赤 | `#FF6678` |
| 5 | 灰 | `#ABB5C6` |

### 2.4 難易度

| 難易度 | 最大敵数 | 出現間隔 | 移動速度 |
| --- | ---: | ---: | ---: |
| EASY | 3 | 2秒 | 22 |
| NORMAL | 5 | 2秒 | 34 |
| HARD | 7 | 2秒 | 49 |

値は `game.js` の `DIFFICULTIES` で管理する。難易度調整時は、この定数を優先して変更する。

### 2.5 操作と状態

| 状態 | ボタン表示 | 動作 |
| --- | --- | --- |
| 開始前 | ゲームを開始 | 新しいゲームを開始 |
| プレイ中 | 一時停止 | 敵の移動・出現・タイマーを停止 |
| 一時停止中 | ゲームを再開 | 停止した状態から再開 |
| ゲーム終了 | ゲームを開始 / もう一度挑戦 | 新しいゲームを開始 |

- スマホでは開始／一時停止ボタンを画面下部に固定する。
- 開始時にはゲームエリアまで自動スクロールする。
- プレイ中は、連続タップ・ピンチ操作・iOSジェスチャーによるズームを抑止する。
- ゲーム終了後はブラウザのズーム操作を通常状態へ戻す。

## 3. 画面構成

### 3.1 PC表示

```text
┌──────────────────────────────────────────────────────────┐
│ CORE DEFENSE                                  v1.x.x      │
├───────────────┬──────────────────────────────────────────┤
│ 防衛レベル    │ TIME        DESTROYED        THREATS      │
│ EASY          ├──────────────────────────────────────────┤
│ NORMAL        │                                          │
│ HARD          │             ゲームエリア                 │
│               │       敵 × → 固定コア ○                 │
│ ミッション    │                                          │
│ [開始/停止]   ├──────────────────────────────────────────┤
│               │ 出現ルールの注記                         │
├───────────────┴──────────────────────────────────────────┤
│ CORE DEFENSE                              v1.x.x          │
└──────────────────────────────────────────────────────────┘
```

### 3.2 スマホ表示

- 幅760px以下では、難易度ボタンを横3列に並べる。
- 開始／一時停止ボタンは常に画面下部に固定する。
- ゲームエリアは高さ `52dvh`、最小330pxとし、画面端が見切れにくい大きさにする。
- フッターは濃紺で表示し、ミント色の操作ボタンと区別する。

## 4. デザイン仕様

### 4.1 デザイン方針

近未来・SF・ミニマルな防衛ゲームUI。

- 濃紺を基調にし、静かな司令室のような背景を作る。
- 守る対象と操作可能な要素はミント色で強調する。
- 危険・敵の状態は耐久に応じた色で示す。
- 細いグリッド、控えめな発光、半透明パネルにより、情報の階層を作る。
- 装飾よりもタップ対象とゲーム状態の見やすさを優先する。

### 4.2 カラーパレット

| 用途 | 色 |
| --- | --- |
| ページ背景 | `#0A101C` |
| 濃色フッター | `#050913` |
| 本文 | `#EBF1FF` |
| 補助テキスト | `#8794AB` |
| パネル | `rgba(20, 31, 51, .86)` |
| 罫線 | `rgba(177, 197, 229, .17)` |
| 主アクセント／コア／主要ボタン | `#6EE7D1` |
| 危険色 | `#FF5D70` |

敵の色は「2.3 敵の耐久と色」を参照する。

### 4.3 タイポグラフィ

| 用途 | フォント | 特徴 |
| --- | --- | --- |
| 見出し・UI本文 | Outfit | 太め、読みやすい、現代的 |
| 数字・ラベル・バージョン | DM Mono | 等幅、計器・システム表示の印象 |

大きな見出しは文字間隔を少し広げ、ラベルは小さな等幅フォントと大文字表記で統一する。

### 4.4 コンポーネントのルール

| 要素 | 表現ルール |
| --- | --- |
| パネル | 半透明の濃紺、細い境界線、角丸16px |
| 難易度選択 | 選択中のみミント色の枠と淡い背景を使う |
| 主ボタン | ミント色の塗り、濃色文字、角丸9px |
| ゲームエリア | 暗い背景、薄いグリッド、内側の境界線、コアを中心に淡い放射光 |
| コア | ミント色の円、複数の同心円、弱い脈動発光 |
| 敵 | 発光する×印。耐久に応じた色を使う |
| フッター | 操作ボタンより濃い紺、細い枠線、等幅フォント |

## 5. ソース構成

| ファイル | 役割 |
| --- | --- |
| `index.html` | 画面のHTML構造、Google Fontsの読み込み |
| `design/tokens.css` | 共通の配色、フォント、角丸などのデザイントークン |
| `design/styles.css` | レイアウト、レスポンシブ表示、コンポーネント見た目 |
| `design/theme.js` | Canvas内のコア・敵の色と発光テーマ |
| `game.js` | Canvas描画、敵の生成・移動、タップ判定、ズーム抑止 |
| `CHANGELOG.md` | バージョンごとの更新履歴 |
| `SPECIFICATION.md` | 本仕様書 |

## 6. 変更時の指針

- 新しい画面・部品を追加する場合は、既存のカラーパレット、2種類のフォント、角丸・罫線・発光の強さを踏襲する。
- 主操作はミント色、危険は赤系、敵の耐久状態は指定された5色を使う。
- ゲームルールを変更したときは、`README.md` と本仕様書を同時に更新する。
- 公開版の変更時は、`CHANGELOG.md` にバージョン・日付・変更内容を追記する。
- GitHub Desktopでコミット・プッシュすると、GitHub Pagesの公開サイトへ反映される。
